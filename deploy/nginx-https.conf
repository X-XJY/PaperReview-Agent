limit_req_zone $binary_remote_addr zone=paperreview:10m rate=5r/s;
server {
    listen 80 default_server;
    server_name 134.175.147.254;
    location /.well-known/acme-challenge/ { root /var/www/html; }
    location / { return 301 https://$host$request_uri; }
}
server {
    listen 443 ssl;
    server_name 134.175.147.254;
    ssl_certificate /etc/letsencrypt/live/134.175.147.254/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/134.175.147.254/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    client_max_body_size 245m;
    location / {
        limit_req zone=paperreview burst=20 nodelay;
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 120s;
    }
}
